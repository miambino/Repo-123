#include <stdio.h>
int main()
{
    printf("Hello World! Did you know that the number of bytes in an int is %lu\n", sizeof(int));
    printf("While the number of bytes in a short is %lu\n", sizeof(short));
    return 0;
}

    //in case you forget, run this with ./ChelloWorld (the exe is "ChelloWorld.exe")